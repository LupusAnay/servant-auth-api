{
  "name": "frontend",
  "version": "1.0.0",
  "dependencies": {
    
  },
  "scripts": {
    "serve": "elm reactor",
    "build:debug": "elm make --output=\"dist/elm.js\" src/Main.elm",
    "build:release": "elm make --optimize --output=\"dist/elm.js\" src/Main.elm"
  }
}
